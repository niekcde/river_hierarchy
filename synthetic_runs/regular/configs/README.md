# Regular Synthetic Configs

These example JSON configs drive the current extracted regular workflow:

- `geometry.example.json`
- `sampling.example.json`
- `routing.example.json`
- `k_metrics.example.json`

Paths are resolved relative to the config file location, so the examples can be
used as templates and edited in place.
